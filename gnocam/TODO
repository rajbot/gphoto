
HELP:
-----
- Do something like this:
   gchar* helpfile;
   
   helpfile = gnome_help_file_find_file("gnocam", "index.html");

   if (helpfile != NULL) {
       gchar* url;
       url = g_strconcat("file:", helpfile, NULL);
       gnome_help_goto(NULL, url);
       g_free(url);
       g_free(helpfile);
   } else {
       gnome_error_dialog(_("Couldn't find the GnoCam manual!"));
   }

Icon, Logo:
-----------
- remain to be created.

Drag & Drop:
------------
- correctly implement drag (files/previews). Timeout issue?

Else:
-----
- Camera properties not yet implemented properly.

Threads:
--------
- Put the time-consuming save stuff in threads. How do I do that?
